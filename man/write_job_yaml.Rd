% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/azure-cli.R
\name{write_job_yaml}
\alias{write_job_yaml}
\title{Write an Azure ML Command Job YAML file}
\usage{
write_job_yaml(
  target_name,
  cluster_command,
  cluster,
  datastore_path,
  environment
)
}
\arguments{
\item{target_name}{Character. The targets target name (used as display name).}

\item{cluster_command}{Character. The R expression string to run via
`Rscript -e`.}

\item{cluster}{Character. Azure ML compute cluster name.}

\item{datastore_path}{Character. Azure ML datastore URI for the targets
cache, e.g.
`"azureml://datastores/workspaceblobstore/paths/pipeline/_targets/"`.}

\item{environment}{Character. Pre-formed Azure ML environment reference, e.g.
`"azureml:r-tidyverse@latest"`.}
}
\value{
Path to the written YAML file (character scalar).
}
\description{
Assembles an Azure ML Command Job YAML with split-storage support and writes
it to a temporary file. The generated job mounts a blob datastore as a
read-write output (`cluster_targets_dir`) and symlinks it to `_targets/`
in the cluster container before running R.
}
\keyword{internal}
