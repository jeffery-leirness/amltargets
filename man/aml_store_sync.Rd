% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{aml_store_sync}
\alias{aml_store_sync}
\title{Refresh the local store with remote cluster results}
\usage{
aml_store_sync(
  account_name,
  container_name,
  cloud_store_base,
  project = Sys.getenv("TAR_PROJECT"),
  link_dir = "data"
)
}
\arguments{
\item{account_name}{Character scalar. Azure storage account name.}

\item{container_name}{Character scalar. Azure storage container name.}

\item{cloud_store_base}{Character scalar. Parent directory of the targets
store on Azure Blob storage.}

\item{project}{Character scalar. Target project/region ID. Defaults to the
`TAR_PROJECT` environment variable.}

\item{link_dir}{Character scalar. Parent directory containing parity
symlinks. Defaults to `"data"`.}
}
\value{
Invisibly, the character path to the updated local targets store.
}
\description{
Resets the local cache, cycle-refreshes the Blobfuse mount to invalidate
stale VFS attribute caches (local compute instances only), re-establishes
parity symlinks, and re-pulls the cloud store.
}
