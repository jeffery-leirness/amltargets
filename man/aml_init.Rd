% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{aml_init}
\alias{aml_init}
\title{Initialize an Azure ML-backed targets session}
\usage{
aml_init(
  account_name,
  container_name,
  cloud_store_path,
  project = Sys.getenv("TAR_PROJECT"),
  blob_link_name = "blob_storage",
  workspace_link_name = "workspace"
)
}
\arguments{
\item{account_name}{Character scalar. Azure storage account name.}

\item{container_name}{Character scalar. Azure storage container name.}

\item{cloud_store_path}{Character scalar. Path suffix under the blob storage
root to the targets store parent directory.}

\item{project}{Character scalar. Target project/region ID. Defaults to the
`TAR_PROJECT` environment variable.}

\item{blob_link_name}{Character scalar. Forwarded to [aml_parity()]. Defaults
to `"blob_storage"`.}

\item{workspace_link_name}{Character scalar. Forwarded to [aml_parity()].
Defaults to `"workspace"`.}
}
\value{
A named list with elements `is_azure` and `cloud_store_base`.
}
\description{
Top-level entry point that establishes blob storage parity (see
[aml_parity()]) and then pulls the cloud targets store into the local
session (see [aml_store_pull()]).
}
