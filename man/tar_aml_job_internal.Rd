% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchestrator.R
\name{tar_aml_job_internal}
\alias{tar_aml_job_internal}
\title{Internal orchestrator for Azure ML target execution}
\usage{
tar_aml_job_internal(
  target_name,
  command_str,
  deps,
  cluster,
  datastore_path,
  environment,
  resource_group,
  workspace,
  poll_interval = 60L
)
}
\arguments{
\item{target_name}{Character. The target name string.}

\item{command_str}{Character. The R expression string.}

\item{deps}{Formula. Embeds the raw command expression for dependency
tracking (`~ <command>`).}

\item{cluster}{Character. Azure ML compute cluster name.}

\item{datastore_path}{Character. Azure ML datastore URI for the targets
cache, e.g.
`"azureml://datastores/workspaceblobstore/paths/pipeline/_targets/"`.}

\item{environment}{Character. Pre-formed Azure ML environment reference,
e.g. `"azureml:r-tidyverse@latest"`.}

\item{resource_group}{Character. Azure resource group name.}

\item{workspace}{Character. Azure ML workspace name.}

\item{poll_interval}{Integer. Seconds between status polls.}
}
\value{
The target value, read from the shared `_targets/` mount after the
  Azure ML job completes (hybrid mode), or the result of evaluating
  `command` locally (cluster/local mode).
}
\description{
Called by the target factory during `tar_make()`. Handles three execution
modes: cluster/local evaluation, job submission, and status polling.
Not intended to be called by users directly.
}
\keyword{internal}
